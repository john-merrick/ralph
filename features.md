# Features Log

Human-readable mirror of features appended mid-project via the `/add-feature`
skill. Newest entries are appended at the bottom; this file is never rewritten.

Each entry: a dated title, a one-paragraph description, and the item ids added to
`prd.json`.

---
